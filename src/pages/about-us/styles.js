import styled from 'styled-components';

export const Body = styled.div`
  background-color: ${(props) => props.theme.color.bluetest};
`;

export const Title = styled.h1`
  color: ${(props) => props.theme.color.redtest};
`;
